name: Build APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Tools
      run: |
        sudo apt-get update
        sudo apt-get install -y libtool pkg-config autoconf automake scons qemu-user-static locales openjdk-17-jdk-headless libffi-dev libssl-dev
        pip install --upgrade buildozer wheel setuptools "cython<3.0.0" virtualenv

    - name: Generate Perfect Buildozer Spec
      run: |
        # Borramos el archivo viejo para que no estorbe
        rm -f buildozer.spec
        
        # Creamos uno nuevo con los parámetros exactos que Android y GitHub necesitan hoy
        cat << 'EOF' > buildozer.spec
        [app]
        title = Mi App Gym
        package.name = miappgym
        package.domain = org.ferpalacios
        source.dir = .
        source.include_exts = py,png,jpg,kv,atlas,json
        version = 0.1
        requirements = python3,kivy
        orientation = portrait
        osx.kivy_version = 2.3.0
        fullscreen = 1
        
        android.api = 33
        android.minapi = 21
        android.ndk_api = 21
        android.archs = arm64-v8a, armeabi-v7a
        android.allow_backup = True
        
        [buildozer]
        log_level = 2
        warn_on_root = 0
        EOF

    - name: Build with Buildozer
      run: yes | buildozer -v android debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MiAppGimnasio
        path: bin/*.apk
